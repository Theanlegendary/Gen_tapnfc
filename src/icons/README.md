# Icons

The active icon registry is currently:

- `src/components/AppIcon.tsx`

Add new icons to `AppIcon.tsx` first so all screens use the same icon style. This folder is reserved for a future dedicated icon registry if the project is reorganized.
